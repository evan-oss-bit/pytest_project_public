tags:
  - 获取模块信息列表
description:
    flasgger进行在线文档生成，获取模块信息列表
parameters:
  - name: body
    in: body
    required: true
    schema:
      id: 10
      properties:
        page_no:
          type: integer
          description: 分页页数
        page_size:
          type: integer
          description: 分页大小
        module:
          type: string
          description: 模块名称
responses:
  200:
     description: 获取模块信息列表成功
     example:
  404:
     description: 获取模块信息列表失败
     example:
